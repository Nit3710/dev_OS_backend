spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devos_dev
    username: devos
    password: devos123
  
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  redis:
    host: localhost
    port: 6379

logging:
  level:
    com.devos: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

devos:
  security:
    jwt:
      secret: devos-development-secret-key-change-in-production
  
  ai:
    timeout: 60000
    retry-attempts: 2
